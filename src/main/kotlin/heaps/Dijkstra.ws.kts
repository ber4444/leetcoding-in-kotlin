// Dijkstra and Bellman-Ford are complicated and won't be asked to implement them in an interview
// you can just know that these are used for calculating sortest paths in weighted graphs (whereas BFS does that in unweighted graphs)
// BF can accommodate negative weights as well, at the expense of running in O(V*E), whereas Dijkstra is faster
